node_modules
dist
.netlify
.env
.env.*
.DS_Store
*.log

# Локальная история нужна только для режима GitHub Actions.
# На Netlify данные хранятся в Netlify Blobs — там этот файл не используется.
# Если используете GitHub Actions как бота — закомментируйте строку ниже,
# чтобы коммитить историю в репозиторий.
data/
